// SMS Integration tab was removed from the Leads dashboard (replaced by the
// Blacklist Leads tab). This module is intentionally empty and no longer wired
// into the tab dispatcher or the dashboard catalog. Safe to delete.
export {};
